package org.example;

import com.netflix.hystrix.HystrixCommand;
import com.netflix.hystrix.HystrixObservableCommand;

public class ProxyFacade {

    public ProxyFacade() {
        //HystrixCommand.Setter key = null;
        //HystrixCommand command = HystrixCommand.Setter.withGroupKey();
        //HystrixObservableCommand observableCommand = new HystrixObservableCommand();
    }

}
