package mayton.probes;

/*
import com.carrotsearch.hppc.cursors.IntCursor;
import grph.Grph;
import grph.in_memory.InMemoryGrph;
import org.slf4j.Logger;
import org.slf4j.LoggerFactory;

import toools.collections.primitive.LucIntSet;
import toools.io.file.RegularFile;
*/

public class AppGRPH {


    public static void main(String[] args) throws Exception {

/*
        // Creating a 4x4 grid and display it in a graphical view:
        Grph g = new InMemoryGrph();
        g.grid(4, 4);
        //g.display();

        // Computing the diameter of the graph:
        int d = g.getDiameter();

        // Writing it on disk, in a file named myfile.txt:
        RegularFile f = new RegularFile("tmp/GRPH.InMemoryGrph.txt");

        f.setContent(g.toGrphText().getBytes());

        LucIntSet vertices = g.getVertices();
*/
    }
}
