(defconstant WIDTH 512)

(listp ...)
(atom ..)
(setf
(let
(cons
(car
(cdr)
(typep "Hello" 'string)

#

(make-hash-table ...)
(hash-table-p ..)
(hash-table-count ...)

#

(defmacro ...)

#

(mapcar #'* '(1 2) '(3 4))

(setq y '(
          (x2 x3 ~x4 x1)
          (~x2 ~x4 x3 x1)
          (x2 x4 x1 ~x3)
          (x2 x4 ~x1 ~x3)
          (~x2 ~x1 x3 x4)
          (~x2 ~x1 ~x3 x4)
          (x2 x3 ~x1 ~x4)
          (x3 ~x2 ~x1 ~x4)))

(defun glue-quine (expr)
   # Inpt: ((x1 x2 x3 ~x4) (x1 ~x2 x3 ~x4).....)
   # Out:  ((x1 x3 ~x4).....)
)

(defun sdnf-expr-count(expr) 
  # In: ........... 
  # Out: 8
)

(defun sort-quine (expr)
   # Input:  (x2 x3 ~x4 x1)
   # Output: (x1 x2 x3 ~x4)
)

(defun quine (expr)
   (let sdnf 
   # Input : y
   # Output: ((x3 ~x4) (x2 ~x3 x4) (~x1 ~x2 x4))
   
)

(quine .....)

